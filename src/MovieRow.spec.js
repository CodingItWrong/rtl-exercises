import {render, screen} from '@testing-library/react';
import MovieRow from './MovieRow';

describe('MovieRow', () => {
  it('NAME AND ADD YOUR TEST HERE', () => {
    // render(<MovieRow ... />);
    // expect(...)
  });

  // add more tests as necessary
});
